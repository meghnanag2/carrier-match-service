{
  "name": "carrier-match-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Vanilla TypeScript frontend for carrier-match-service — no framework, no build step beyond tsc.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
